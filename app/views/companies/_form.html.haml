= simple_form_for @company do |f|
  = f.error_notification
  = f.input :name
  = f.input :url
  = f.input :logo, as: :file

  - if @company.logo.attached?
    .image-preview
      = image_tag(@company.logo, alt: 'Logo atual')

  = f.button :submit, class: 'btn btn-primary btn-block'
